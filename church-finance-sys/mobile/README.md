# Church Finance Mobile App

## Flutter Required
The Flutter CLI was not found in the environment during project initialization.

## Initialization
To initialize the mobile application, please ensure Flutter is installed and run:

```bash
flutter create .
```

This will generate the Android and iOS project files in this directory.
